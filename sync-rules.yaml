## Example sync rules that work with example mongodb instance generated by `setup-mongodb.sh` script
bucket_definitions:
  buckets:
    parameters: select id from users where name = token_parameters.name
    data:
      - select * from products where author = bucket.id
      - select * from users where _id = bucket.id
      - select * from orders where userId = bucket.id
